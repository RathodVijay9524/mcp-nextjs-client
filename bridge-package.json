{
  "name": "mcp-bridge-server",
  "version": "1.0.0",
  "description": "Local bridge server to connect deployed web app with local MCP servers",
  "main": "local-mcp-bridge.js",
  "scripts": {
    "start": "node local-mcp-bridge.js",
    "dev": "nodemon local-mcp-bridge.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
